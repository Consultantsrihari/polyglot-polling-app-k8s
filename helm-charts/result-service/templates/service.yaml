apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-result-service
spec:
  type: ClusterIP
  selector:
    app: result-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http